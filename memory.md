# Project Memory: Mr. Polish Website State

## Current Context
We are starting the step-by-step development of the Mr. Polish business website from scratch in an empty repository.

## What We've Built So Far
- Initial project structure initialized in Antigravity IDE.
- Centralized rule definition and bilingual requirements established via `CLAUDE.md`.
- **Brand assets:** Official logo in place as `logo.jpg` (royal navy + metallic gold on marble). Note: original upload was `logo.jpg.jpg` (double extension) and was renamed.
- **Palette (final):** Per client request, **Deep Charcoal + Metallic Gold** is the dominant scheme (OKLCH `--char-*` / `--gold-*` in `styles.css`). Navy kept only as a secondary accent (`--navy-*`) for logo harmony, since the logo itself is navy+gold on marble.
- **UI polish (Step 2):** Entrance animations via `.reveal` class + `IntersectionObserver` in `main.js` (gated by `.js` on `<html>`, set by an inline head script so no-JS users still see content and there's no flash). Hero content animates on load (staggered). Buttons have a hover shine sweep + lift + gold glow; primary CTA has `.btn-pulse`. Cards lift with a growing gold top-bar; FAQ/nav/footer hovers refined. Respects `prefers-reduced-motion`.
- **Scroll-Reveal engine (upgraded):** `.reveal` base = `opacity:0; filter:blur(4px); transform:translateY(30px)`; reveal trigger class renamed `is-in` → **`.active`** = `opacity:1; filter:blur(0); transform:none`, transition `0.8s cubic-bezier(0.16,1,0.3,1)` on opacity/transform/filter. `IntersectionObserver` (main.js §5) adds `.active` once at **threshold 0.15** then unobserves; no-IO fallback adds `.active` to all; still `.js`-gated; reduced-motion clears blur too. Applied to all section-heads, service cards (staggered), skew cards, FAQ tabs+panels, gallery sliders, about, contact form (hero excluded — own load anim). Verified: lingering `filter:blur(0)` does NOT break skew-card `backdrop-filter` glass; 0 console errors.
- **Mobile fix:** Resolved a horizontal-overflow bug — `.about-media` (320px logo + 40px padding) forced its grid track past the viewport. Fixed with `min-width:0` on grid children + responsive padding/img max-width. Verified `scrollWidth === clientWidth` (overflow 0) at 375px.
- **Splash/Preloader (extra, client-requested):** `preloader.mp4` (Higgsfield branded clip, 1920×1080, ~4.06s) in root. Full-screen fixed overlay (`.preloader`, z-index 9999, `100vw`/`100vh`+`100dvh`); video uses `object-fit: cover` to fill the screen (verified video rect == viewport on desktop 1280×800 and mobile 390×844, overflow 0).
  - **Animated branding container:** `.preloader-content` > `.preloader-brand` (column: logo + name) starts `opacity:0` and fades/rises in together via `.preloader.show-name .preloader-brand`. `.preloader-logo` = `logo.jpg`, `clamp(140–230px)`, rounded, soft dark+gold glow box-shadow. `.preloader-title` = "Mr. Polish | מיסטר פוליש": container `direction:ltr` keeps EN|HE order; `.pt-he` is `direction:rtl; unicode-bidi:isolate` for strict Hebrew RTL. Gold metallic gradient via `background-clip:text` (OKLCH `--gold-*`), drop-shadow glow + `ptGlow`/`ptShimmer` animations on `.show-name`.
  - **Timeline (main.js section 0):** anchored to video `playing` (fallback 400ms). Name fades in at **3s** (also cued by `timeupdate` ≥3s), full reveal at **5s** → `.is-hidden` = opacity 0 + `translateY(-100%)` over **0.8s**, then `display:none`/`aria-hidden`. Locks/restores body scroll. Robustness: skip button, 7s ultimate safety timeout, `<noscript>` hide when JS off, reduced-motion shortcut (~1.2s). Verified end-to-end, zero console errors.
- **Contact:** Phone & WhatsApp = 052-9534540 (intl `+972529534540`, `wa.me/972529534540`).
- **Homepage shipped** (`index.html` + `styles.css` + `main.js`), flat folder structure. Verified rendering on desktop + mobile and confirmed English LTR switch does not break layout. No console errors.
  - Sections: sticky header w/ logo, Hero (30-yr badge, dual call/WhatsApp CTA, trust strip), Services (6 cards), interactive Before/After sliders (placeholder Unsplash imagery), About, Lead form, FAQ accordion, footer.
  - Conversion: sticky mobile bottom action bar, lead form routes submissions to WhatsApp (no backend yet), honeypot spam guard, consent checkbox linking to privacy.html.
  - i18n: `data-i18n` keys + JS dictionary, full HE/EN switch toggling `dir`/`lang`, preference saved to localStorage. All CSS uses logical properties.
  - SEO: meta tags, OG tags, LocalBusiness JSON-LD schema.

## Architectural Decisions
1. **Hebrew-First Mobile UX with English Support**: Website is built primarily in Hebrew (RTL) with a rigid mobile-first approach, prepared structurally for full English (LTR) translation switching.
2. **Component-Driven Milestones**: We will build the site page-by-page, stabilizing the Homepage and visual galleries before implementing any backend or admin panel logic.

## Current Task Checklist (Next Steps)
- [x] Step 1: Semantic HTML structure for the Homepage (`index.html`) — Hero, 30 Years badge, Lead Capture Form. **Done.**
- [x] Step 2: Styling with modern CSS / OKLCH colors, interactive Before/After sliders, FAQ accordions. **Done** (`styles.css`).
- [x] Step 3: Conversion features **DONE**. Mobile bottom action bar = floating glassmorphism pill (`.mobile-bar`, mobile-only, hidden ≥860px), 3 gold buttons: חייג עכשיו `tel:0529534540` / שלח וואטסאפ `wa.me/972529534540` / ניווט בוויז `waze.com/ul?q=Mr.%20Polish`. Quick Share = `.share-fab` (Lucide share-2 SVG, bottom-inline-start, lifts above bar on mobile) → `navigator.share` with WhatsApp fallback (`initShare` main.js §8) + toast; share text "היי, תראה את האתר הזה לפוליש...". **Thank-You page** `thankyou.html` (charcoal/gold, animated gold checkmark + confetti, header "תודה! הפרטים התקבלו בהצלחה", gold home button). Lead form now delivers to WhatsApp THEN redirects to `thankyou.html` after 400ms (Google Ads conversion anchor). Verified mobile+desktop, 0 errors.
- [x] Step 4: Legal pages **DONE** — `accessibility.html`, `privacy.html`, `terms.html` created. Shared minimalist `.legal-header`/`.legal-hero`/`.legal-content`/`.legal-footer` CSS (charcoal+gold, `.reveal`, shimmer back buttons in header + bottom). Each reuses styles.css + main.js + `.js` inline flag. Hebrew-only (no lang switch). Privacy includes the mandatory **סעיף 13 לחוק הגנת הפרטיות התשמ״א-1981** (inspect/correct/delete rights) in a highlighted box. Accessibility cites WCAG 2.1 AA + ת"י 5568, lists adjustments, Uri as accessibility coordinator. Terms covers usage, IP (Uri's techniques/photos/logo), liability limits, Israeli jurisdiction. Footer + form-consent links in index.html already pointed correctly. Fixed an HTML bug: invalid `\"` in privacy meta → Hebrew gershayim ״. All anchors + local assets resolve (except intentional before/after placeholders). 0 console errors on all pages.
- [x] Step 5: Admin Portal **DONE** — `login.html` + `admin.html` (self-contained: link styles.css for tokens/buttons, inline `<style>`+`<script>`). Charcoal/gold design.
  - **Auth (Supabase Auth — real server-side login):** login.html loads `@supabase/supabase-js@2` (CDN), uses `signInWithPassword({email,password})`; supabase-js stores/refreshes the session in localStorage (key `sb-jxawixeegaaqjdbtacfi-auth-token`). login.html auto-redirects to admin.html only if `getUser()` validates a real session (server-side, avoids tampered-token loops). admin.html: fast head pre-check for the token key (anti-flash) + body `getUser()` verification → bounce to login.html if invalid/expired; logout = `sb.auth.signOut()`. Admin user must be created in Supabase Dashboard → Authentication → Users. (Replaced the old hardcoded client-side password, which was insecure.)
  - **Dashboard tab:** 4 count-up metric cards + recent-activity list (mocked).
  - **Calendar tab:** RTL month grid, click day → editor, notes saved to `localStorage.mrp_cal_notes` (JSON by `YYYY-MM-DD`), gold dot on days with notes, today outlined, prev/next month. Persistence verified.
  - **Tab 3 = "הזמנות עבודה" (work order)** — REDESIGNED (replaced the old free-text AI parser/double layout). Nav label + topbar title = "הזמנות עבודה" (still `data-view="ai"` / `#view-ai`). Two-column `.order-layout` (desktop 1.5fr/1fr; stacks on mobile):
    - **Right = `.order-form`** (charcoal/gold): fields `ofNum` (readonly, auto), `ofDate` (prefilled `toLocaleDateString("he-IL")`, editable), `ofClient` (עבור הלקוח), `ofAddress` (כתובת), `ofDesc` (textarea), `ofTotal` (סך הכל לתשלום, ₪ prefix). Button `ofGenerate` "הפק הזמנת עבודה (PDF)".
    - **Left = `.calc-card`** helper calculator: `calcPrice` × `calcMeters` → live glowing gold `calcTotal` (bump anim on input); `calcSync` button "עדכן סכום בטופס" copies rounded total into `#ofTotal` (with gold highlight flash).
    - **Auto order number:** `localStorage.mrp_order_seq` (default 3 → "003", `pad3`), increments by 1 ONLY after a successful PDF generate; `ofNum` updates live.
    - **Direct PDF download:** html2pdf.js via CDN (cdnjs 0.10.1, bundle) in `<head>`. `ofGenerate` builds `#orderDoc` (`.wo`, off-screen `position:fixed; left:-10000px; width:794px`) then `html2pdf().set(opt).from(...).save()` → file `הזמנת_עבודה_NNN.pdf`, A4 portrait, html2canvas scale 2. NO `window.print()` / no print dialog. Logo via `resolveLogo` png→jpg→none.
    - **Document matches Uri's PAPER form:** בס"ד; brand "Mr. Polish" + sub "כל עבודות הליטוש, פוליש והברקה קריסטלית"; contact "אורי: 052-9534540", "רח׳ אהרון אהרונסון 15, נתניה", **"עוסק מורשה 023062185"**; titlebar הזמנת עבודה + מס׳ + תאריך; עבור הלקוח/כתובת; תיאור block; total; **תנאי תשלום hardcoded** (50% ביום ההגעה / 40% בסוף העבודה / 10% לפינישים הסופיים / הצעת המחיר אינה כוללת מע"מ); signatures חתימת הלקוח/חתימת המבצע; greeting "בברכה, אורי מרגלית, Mr. Polish".
    - ✅ **STATUS RESOLVED:** Uri is **עוסק פטור**. Header reads "עוסק פטור 023062185" (same ID), and "הצעת המחיר אינה כוללת מע"מ" stays (no VAT charged). Do NOT revert to עוסק מורשה or add VAT. Old `qd-*` quote CSS/`#quoteDoc`/window.print fully removed.
  - Verified: guard, login, all 3 tabs, calendar persistence, work-order form + live calc (75×60=₪4,500) + sync, html2pdf loaded, order# 003, date prefill, two-column desktop layout, **0 console errors**.

- **Gradient Button (rotating border, from 21st.dev React):** Converted to semantic `<button>` `.btn-gradient` in `styles.css`. Background = `conic-gradient(from var(--r), gold/charcoal stops)`; inset `::after` (inset 5px, z-index 1, fill `--char-900`) leaves a rotating gradient **border ring**; `.label` span at z-index 2 holds the text + `data-i18n`. Rotation via registered `@property --r` (`<angle>`) + `@keyframes rotateR { to { --r:360deg } }`, 4s linear. Hardcoded 600×100 dropped → fully responsive via `.btn`/`.btn-block` (measured 553px desktop / 286px mobile, overflow 0). Overrides base `.btn::after` hover-sweep (`transform:none` + `:hover::after`). **ADDED alongside** shimmer + shiny (none replaced). Applied to the **lead-form submit** (high-intent). NOTE: form submit is no longer `.btn-shimmer`; shimmer still used on header call + hero WhatsApp.
- **Shiny Button (from Magic UI `<ShinyButton>`, Framer Motion):** Converted to `.btn-shiny` in `styles.css`. Framer's animated `--x` (100%→-100% loop w/ repeatDelay) → registered `@property --x` (`<percentage>`, inherits) driving `@keyframes shinyX` (0%:100% → 60%:-100% → 100%:-100% hold ≈ repeatDelay) over 3s. Gold metallic streak across the TEXT via `.shiny-content` `mask-image: linear-gradient(-75deg, #000 calc(var(--x)+20%), transparent +30%, #000 +100%)` and across the BORDER via `::before` padding-ring `mask-composite: exclude`. Glassy: gold radial wash + `backdrop-filter: blur(16px)`, white text, hover gold glow. No uppercase (Hebrew), RTL-safe. **ADDED alongside** the shimmer button (not replacing it). Applied to the **hero Call** button (markup needs `<span class="shiny-content">` wrapper) to contrast with the green `.btn-shimmer.is-wa` WhatsApp beside it. Header call + form submit remain `.btn-shimmer`.
- **Shimmer Button (from 21st.dev `<ShimmerButton>`):** Converted React/Tailwind → `.btn-shimmer` class in `styles.css`. Charcoal base `linear-gradient(110deg,...)` with a bright accent band, `background-size:200% 100%`, animated by `@keyframes shimmer2` (background-position 0 → -200%). Gold accent by default (`--shimmer-accent: --gold-500`, gold text/border/glow); `.is-wa` modifier swaps accent to WhatsApp green to preserve identity. Hover-sweep `::after` disabled on shimmer buttons. Applied to core CTAs: hero Call (`.btn-shimmer`) + hero WhatsApp (`.btn-shimmer.is-wa`), header call, and the lead-form submit. RTL-safe (sweep is purely horizontal/visual). Secondary navy buttons (`about_btn`, `srv6_btn`) intentionally left as-is to keep hierarchy. Pill radius kept for site consistency (source used `rounded-md`).

- **Flagship Skew Cards (glassmorphism + ambient glow, from 21st.dev React):** New `.skew-section` added in `index.html` BETWEEN the existing 6-card services grid and the gallery — a "שירותי הדגל / Flagship services" spotlight (kept the existing grid; this is additive). Converted React `.map` → 3 semantic `<article class="skew-card skew-card--1/2/3">` each with `__panel` (conic? no—`linear-gradient(315deg,var(--from),var(--to))` skewed), `__glow` (blurred twin), `__blobs` (2 glass bubbles on hover), `__content` (glass: `oklch(1 0 0/.06)` + `backdrop-blur`), `__cta` (gold pill). Neon→metallic: card1 gold→bronze, card2 bronze→charcoal, card3 gold→deep-bronze (OKLCH). **RTL:** skew + offsets mirrored via logical props (`inset-inline-start`) + per-dir vars `--skew:-15deg/--shift:25px` (RTL) flipped under `[dir="ltr"]`. Dropped no fixed sizes issue (cards `width:min(320px,100%)`, content `min-height:400px`). Hover: panel straightens+widens, content shifts `translateX(var(--shift))`, bubbles animate (`@keyframes blob`). Verified desktop+mobile, hover interaction, overflow 0, bilingual (EN keys added), 0 console errors.
  - Card1 desc copy fixed: "מראה מראה נוצץ" → "מראה נוצץ" (doubled-word typo corrected).

- **Tabbed FAQ Accordion (from 21st.dev React/Framer):** New `#faq-tabs` section added in `index.html` after the flagship skew section (under the services area). Converted to vanilla JS (`initFaqTabs` in `main.js` section 6): category tabs (`role=tablist`, 2 tabs — "על התהליך והשירות" / "מחירים והזמנות") switch panels via `is-active`/`hidden`/`aria-selected`; questions toggle `.is-open` + `aria-expanded`. **Smooth height** via CSS `grid-template-rows: 0fr → 1fr` (no JS measuring). **RTL:** `text-align:right`, `justify-content:space-between` puts question text at start (right) and the plus icon at inline-end (left); plus icon = 2 bars via `::before/::after`, rotates 45° to × on open. Gold/charcoal styling: active tab gold gradient + glow, open item gold border. 4 Q&As injected (2 per category, exact client Hebrew) + EN translations. Verified tabs+accordion on desktop/mobile, icon-on-left, overflow 0, 0 console errors.
  - RESOLVED: old simple `#faq` section removed; all 3 nav links (main nav, mobile menu, footer) now point to `#faq-tabs`. (Dead leftovers, harmless: unused `.faq`/`.accordion`/`.acc-item` CSS in styles.css and `faq_*` EN keys in main.js — can be cleaned later.)
  - **Content enriched (current):** Tab1 "על התהליך והשירות" = 3 Q&As (`faq_p_q1/a1` dust/wet-honing, `faq_p_q2/a2` crystal-vs-wax NEW, `faq_p_q3/a3` furniture). Tab2 "מחירים והזמנות" = 2 Q&As (`faq_pr_q1/a1` when-to-walk NEW, `faq_pr_q2/a2` renewal-frequency). HE in HTML + EN in main.js dict (all keys updated). **FAQPage JSON-LD** added in `<head>` (2nd ld+json after LocalBusiness) with all 5 Q&As in Hebrew — keep in sync if FAQ text changes. Verified: 3+2 items, schema valid (5 Qs), tabs/accordion work, EN i18n syncs, 0 console errors.

- **Before/After Comparison slider (from 21st.dev React):** New `#compare` section in `index.html` placed between the flagship skew section and `#faq-tabs`. Converted React state/refs → vanilla pointer-events (`initCompare` in main.js §7: mouse+touch+pen). `.compare` widget uses CSS `--pos` (default 50%); base layer = AFTER (`after-floor.jpg`), overlay = BEFORE (`before-floor.jpg`) clipped via `clip-path: inset(0 calc(100% - var(--pos)) 0 0)` → left=before, right=after. Drag starts on handle (pointer capture), tap/click anywhere jumps, ArrowLeft/Right/Home/End keyboard (role=slider + aria-valuenow). Gold divider + circular gold handle (`flex-shrink:0` — needed it, flex divider was squishing it to an oval), `comparePulse` animation, scale-up on `.is-dragging`. Badges: לפני physical bottom-left, אחרי physical top-right (physical L/R so they don't flip in RTL); reuse `ba_before`/`ba_after` keys. `.reveal` on header + widget. `touch-action: pan-y` on widget (vertical scroll free), `none` on handle.
  - **IMAGES MISSING:** `before-floor.jpg`/`after-floor.jpg` not in repo → graceful CSS gradient placeholders show (muted charcoal=before, shiny gold=after) via `onerror` hiding broken imgs. Two harmless 404s in console until Uri adds the files. Verified drag/keyboard/placeholders/mobile (overflow 0).
  - RESOLVED: old `#gallery` section + `.ba-slider`s fully removed (HTML, dead `.ba-*`/`.gallery-note` CSS, dead `[data-ba]` JS block, dead `gallery_*` EN keys). Nav "לפני ואחרי" (3 links) repointed `#gallery` → `#compare`. Kept `ba_before`/`ba_after` keys (used by compare badges).
- **Testimonials Marquee (קרוסלה אינסופית):** `#testimonials` under `#faq-tabs`. HTML holds ONLY the 3 base `.testi` cards; `initMarquee` (main.js §8b) clones them.
  - **WIDE-SCREEN GAP — ROOT CAUSE & FINAL FIX:** the void was an RTL bug — `.marquee` inherited `dir=rtl`, so the track right-aligned and `translateX(-50%)` pulled it left leaving a void on the right. FIX = **`.marquee { direction: ltr; }`** (CRITICAL — left-aligns track so -50% scrolls left feeding from the right; cards keep their own `direction:rtl`). Replaced the fragile JS width-measuring with a **hardcoded multiplier**: clone base set `REPEAT = 8` times (24 cards; MUST stay even so the two halves are identical) → one half (12 cards ≈4848px) overflows 4K, so `@keyframes marqueeScroll` 0→-50% is always gapless (verified 2560px frozen at -45%: full edge-to-edge, no void). No JS width math, no resize rebuild needed. Track hardened: `display:flex; flex-direction:row; flex-wrap:nowrap; white-space:nowrap`, `.testi { flex:0 0 auto }`. Fixed `animation: marqueeScroll 60s linear infinite`. Clones i>0 get `aria-hidden`.
  - Pause: CSS `:hover` + JS `.is-held` on touch `pointerdown`→remove on up/cancel/leave. Edge-fade `mask-image`, GPU transform, `will-change`. i18n `applyLang` re-queries live `[data-i18n]` so cloned cards translate on HE/EN switch. (Do NOT remove `.marquee{direction:ltr}` or make REPEAT odd.)
  - **Text-wrap fix:** cards inherited the track's `white-space:nowrap` → text collapsed to one overlapping line. Fixed by `.testi { white-space:normal; overflow-wrap:break-word; word-wrap:break-word; }`.
  - **Google reviews CTA:** centered `.reviews-cta` pill under the marquee (inside `#testimonials`, `.reviews-cta-wrap` = `.container` text-align center). `data-i18n="reviews_cta"` (HE "לקריאת כל הביקורות וכתיבת המלצה בגוגל 🌟" / EN "Read all reviews &amp; write a review on Google 🌟"). Gold-bordered glass pill, hover `scale(1.05)` + gold glow. **`href="#"` is a PLACEHOLDER** — HTML comment marks it: replace with Uri's Google My Business / Maps review link. Verified centered, bilingual, overflow 0, 0 errors.
  - **6 unique reviews** (HTML base cards w/ `data-i18n` testi1–6 quote+author, EN translations added): Levi/Ra'anana, Aviv Cohen/Herzliya, Ronit Dan/Netanya, Yuval Gal/Kfar Saba, Shani&Maor/Tel Aviv, Tomer Azoulay/Ra'anana. `REPEAT=4` (6×4=24 cards). Kept cards in HTML (not a JS array) to preserve the bilingual i18n toggle. Verified: text wraps within cards, 24 cards, overflow 0, 0 console errors.

- **Dynamic Multi-Project Gallery (`#compare`):** `initCompare` (main.js §7) now holds a `PROJECTS` array (5 projects, paths `assets/gallery/projectN-before/afterM.jpg`): P1 "אבן בזלת שחורה" (3 angles), P2 "אבן ירושלמית" (2), P3 "רצפת טרצו" (1), P4 "חידוש מדרגות חלילה" (2), P5 "שיש קרמה - בית חולים מאיר" (1). Horizontal-scroll tab bar `#galleryTabs` (`.gallery-tab`, hidden scrollbar, gold active) above slider; angle dots `#galleryDots` (`.gallery-dot`) below — shown ONLY when a project has >1 angle. Imgs `#cmpBeforeImg`/`#cmpAfterImg` (no static src; old before-floor/after-floor refs removed). `loadImg` preloads via `new Image()` then swaps src on success / hides on error (gradient fallback); `loadPair` fades via `.compare.is-swapping` (opacity .35s) and resets `--pos` to 50%. Pointer drag/keyboard unchanged & verified. 
  - **Lazy preloading (perf, main.js §7):** `preloadProject(i)` (`new Image()` cache-warm, guarded by `preloaded{}` so each project fetches once). Init only warms Project 1 (via `selectProject(0)`→`preloadProject(0)`) = 6 images; Projects 2–5 deferred. Tabs prefetch on `pointerenter`+`focus` (hover/touch/keyboard); `selectProject` also calls `preloadProject` on click. Display still via `loadPair`/`loadImg` (fade `.is-swapping` + gradient fallback, no layout shift). Verified: initial gallery footprint 18→**6**, hovering a tab loads only that project's imgs, deferred tabs unloaded until interacted, click loads+displays deferred project, 0 console errors.
  - **Image extension = `.jpeg`** (not `.jpg`): config paths updated to `.jpeg`. Uri's REAL photos are now in `assets/gallery/` as 18 `.jpeg` files (e.g. project1-before1.jpeg, ~1204×1600 progressive JPEGs) and render correctly. (The earlier Pillow `.jpg` demo placeholders are gone/replaced.) Verified: real photos load (project1 = black basalt before/after), 5 tabs, dots 3/2/1/2/1, tab+dot switching, drag works, overflow 0, **0 console errors**.

- **Hero background = looping video** (`index.html`): `<video class="hero-video" autoplay muted loop playsinline preload="auto" poster=<unsplash floor>>` before `.hero-overlay`. **Source = LOCAL `assets/hero-bg.mp4`** (Uri's Higgsfield clip, 1920×1080, 2.05 MB, warm marble interior + bright gold light streak). The local file wasn't present so it was downloaded from the prior CloudFront URL into `assets/hero-bg.mp4`. Verified actually PLAYING from the local path (readyState 4, not paused, currentSrc=assets/hero-bg.mp4). CSS layering: `.hero-video` `position:absolute; inset:0; object-fit:cover; z-index:1`; `.hero-overlay` z-index:2; `.hero-inner` z-index:3. `.hero` keeps unsplash floor image as static fallback (first paint / blocked / reduced-motion `@media reduce{.hero-video{display:none}}`).
  - **Overlay (brightened per feedback — too dark before):** `linear-gradient(to top, rgba(0,0,0,.45), .25 40%, .1 100%), linear-gradient(to right, rgba(0,0,0,.05), .3 60%, .5 100%)` — max opacity 0.5 so the marble video reads bright; text stays legible via the shadow stack. Text shadow `1px 1px 2px rgba(0,0,0,.3), 0 2px 5px rgba(0,0,0,.7)` on h1/sub; `.hero-trust` `0 1px 3px rgba(0,0,0,.55)`. Buttons (gold `.btn-shiny` + green `.btn-shimmer.is-wa`) at z3, overlay `pointer-events:none` so they're interactive (hit-tested). Verified desktop+mobile, overflow 0, 0 console errors.
  - **Header reworked to transparent + scroll state (per feedback — disliked white header):** `.site-header` now `background:transparent` at top (no border/shadow); `.site-header.scrolled` = translucent light bar (`oklch(0.99 0 0/.92)` + blur + border + shadow) toggled by `initHeaderScroll` (main.js §9) when `scrollY>40` — keeps dark nav text readable over BOTH the bright hero (top) and dark sections (scrolled). Nav text stays dark (`--ink-900`). `.header-call` reverted to `btn-shimmer` (removed the white-cube override).
  - **Hero white-gold CUBE — REMOVED (user hated it, blocked the video).** Reverted: `.hero-cube` wrapper deleted from HTML + CSS; badge/h1/`.hero-sub` sit directly on the video again with the base white text + text-shadow stack (`.hero h1` color `oklch(0.99 0 0)`). Overlay stays brightened (max **0.35**).
  - **Hero phone CTA = gold CAPSULE matching WhatsApp** (`.hero-call-btn`, markup `btn btn-lg hero-call-btn`). Final state (user reverted the 4px bar): pill `border-radius:999px` (inherits `.btn`), same `.btn-lg` height/padding as the green WhatsApp button. Gold base + one bright highlight band: `background: linear-gradient(110deg, gold 0–42%, oklch(0.95 0.06 95) 50%, gold 58–100%); background-size:200% 100%; animation: shimmer2 2.6s linear infinite` — reuses the SAME `shimmer2` keyframe as `.btn-shimmer` (infinite light-sweep, band sweeps then "rests" = glide/pause/repeat). Text+icon charcoal `#1e1e1e`, spans `z-index:2`; `::after { display:none }` (drops base hover-sweep). Hover `scale(1.05)` + gold glow. (`.btn-shiny` still used by login/admin.) Verified: radius/padding/animation all match WhatsApp, gold shimmer loops, overflow 0, 0 console errors.
  - **Header phone "cube" (`.header-call`, desktop ≥860px) [REVERTED — see above]:** removed `btn-shimmer`; restyled to WHITE bg, gold text+icon `oklch(0.7 0.15 88)`, 2px gold border, shadow `0 4px 6px rgba(0,0,0,.1), 0 1px 3px rgba(0,0,0,.08)`, hover `scale(1.05)`. Uses `.header-call.header-call` specificity to beat `.btn`; base `::after` shine disabled. Content kept as the real phone **052-9534540** (user mentioned "023062185" but that's the Osek Patur tax ID, NOT a phone — did not use it). Header bg is light, so the cube is defined by its gold border+shadow.
- **Hero contrast fix (superseded by video bg above, shadows kept):** white text was blending with bright floor reflections. Added `text-shadow: 1px 1px 1px rgba(0,0,0,.15), 0 1px 3px rgba(0,0,0,.5)` to `.hero h1` + `.hero-sub`. Styled the previously-empty `.hero-overlay` div = `position:absolute; inset:0; z-index:1; linear-gradient(to right, rgba(0,0,0,.1) 0%, .6 50%, .8 100%)` (darkens physical right); `.hero-inner` bumped to `z-index:2`. Button pop: `.btn-shiny` border 1px→2px gold + stronger hover glow (`--gold-glow`); `.btn-navy` got a faint gold border + gold-glow hover. Verified desktop+mobile readable, overflow 0, 0 console errors.

- **Services section expanded to 12 cards (4×3 grid):** `.cards-grid` now has 11 white `.service-card`s + 1 dark `.card-cta` last. Keys `srv1_*..srv11_*` (title+text) + `srvcta_title/text/btn` (EN added to main.js dict). Services: 1 ליטוש מרצפות וחידוש ריצוף ✨, 2 פוליש קריסטל והברקה 💎, 3 ליטוש שיש ואבן טבעית 🪨, 4 ליטוש וחידוש בטון 🏗️, 5 בומנייט 🧱, 6 ליטוש מדרגות וספי חלון 🪜, 7 חידוש וליטוש טרצו 💠, 8 חידוש וניקוי גרנוליט 💦, 9 קרצוף והחלפת רובה 🧽, 10 מריחת סילר הגנה 🛡️, 11 ניקיון לפני אכלוס 🧹. CTA button text = "לבדיקה" / "Request Inspection" (removed "חינם"). **Lead-form `<select>` decoupled** from the srv keys (was reusing srv1-5 which got remapped) → new dedicated keys `opt_floor/opt_crystal/opt_marble/opt_concrete/opt_stairs/opt_sealer` (+ `form_service_other`), HE+EN. Grid responsive 1/2/3 cols (existing). Verified: 12 cards, 3 cols, CTA last w/ "לבדיקה", EN toggle works for all new keys, overflow 0, 0 console errors.

- **Nationwide trust copy (regions removed):** `trust_nationwide` (hero) + `contact_area` (contact + footer pin section) → "שירות בפריסה ארצית מלאה" / EN "Full Nationwide Service" (was "שירות ארצי - מרכז, ירושלים ובית שמש"). HERO trust bar: restored the leading gold `✓` icon span so all 3 items match (`.hero-trust span[aria-hidden]` = gold `oklch(0.88 0.1 90)`, fw900); trust_nationwide text/EN have NO trailing ✔ (icon provides the check). `contact_area` (the 📍 pin section) keeps its "✔" in text and was NOT touched. Verified 3 identical gold checks, no wrap, overflow 0, 0 errors. NOT changed (out of scope, flag if needed): `CLAUDE.md` business-context line still names regions; head JSON-LD `areaServed` still lists מרכז/ירושלים/בית שמש; SEO meta description mentions השרון/המרכז.

- **Nav order = mirrors scroll:** header main-nav + mobile-menu + footer-nav reordered so R→L matches page top→bottom: **services → compare → faq-tabs → about → contact** (swapped about/faq from before). NOTE: user's numbered list had about before FAQ, but the PAGE physical order is faq-tabs BEFORE about (sections: services, compare, faq-tabs, testimonials, about, contact) — so to truly mirror scroll, nav must have FAQ before About (done). If About-before-FAQ is ever wanted, either revert nav or move the About section above #faq-tabs. All anchors resolve, overflow 0, 0 errors.

- **Contact section premium pass:** (1) `.ci-icon` emoji (📞💬📍, rendered pink/magenta) replaced with Lucide inline SVGs (`stroke=currentColor`); `.ci-icon { color: oklch(0.7 0.15 88) }` gold + `.ci-icon svg { 22px }`. (2) `contact_title` → "קבלו הצעת מחיר מותאמת אישית" / EN "Get a personalized quote". (3) Submit button: class `btn-gradient`→`form-submit-btn` (solid gold `oklch(0.7 0.15 88)`, text `#1e1e1e`, hover `translateY(-2px)`+brightness+gold glow — NOT scale, since btn-block full-width scale causes horizontal overflow). Label `form_submit` → "שליחת פנייה וחזרה אליי 🌟" / EN "Send request &amp; I'll get back to you 🌟". Verified gold icons, new title, gold button, hover no-overflow, 0 console errors.

- **About section (#about) premium refresh:** updated existing `#about` IN PLACE (kept its position before #contact, which the nav mirrors — did NOT move it to "after services grid" to avoid desyncing the nav-scroll order; flag if relocation wanted). `about_title` → "אורי מרגלית – מומחה הליטוש והפוליש שלכם"; `about_p1` → the long authoritative 30-yr paragraph; removed `about_p2` + the `.about-points` list (EN keys about_p2/about_pt1-4 now unused, harmless). Kept eyebrow (gold), logo media + "30+ שנות מומחיות" badge, "דברו איתנו" CTA. Added gold accent line under title (`.about-heading::after`) + airier `.about-text p` (line-height 1.9, 1.05rem). EN updated (about_title/about_p1). Verified desktop 2-col + gold accent, mobile 1-col, EN works, overflow 0, 0 console errors.

## Open Items / Reminders
- Replace placeholder Unsplash gallery images with Uri's real before/after photos.
- Create the three legal pages (links already wired from homepage + footer).
- Not a git repo yet — run `git init` + first commit when ready (CLAUDE.md rule #5).
- A `tel:`/WhatsApp form flow has no server-side capture; revisit when admin panel/backend is built.
